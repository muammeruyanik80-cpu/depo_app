name: Build Kivy APK

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Kodları çek
        uses: actions/checkout@v3

      - name: Python kurulumu
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Buildozer ile APK üret
        run: |
          sudo apt update
          sudo apt install -y buildozer
          buildozer android debug

      - name: APK'yı kaydet
        uses: actions/upload-artifact@v3
        with:
          name: depoapp-debug.apk
          path: bin/*.apk
